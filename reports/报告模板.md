# IEEE-CIS 电商交易反欺诈 · 结项报告（模板）

> 阶段 7 产出。填写说明：把 `reports/run_log.txt` 里的真实数字和
> `reports/figures/` 里的图填进对应位置；所有 "X" 占位都要换成真实结果，
> 每条假设都要给出"结论 + 图"。

---

## 1. 业务痛点

电商支付场景中，欺诈交易（盗卡、账户接管、团伙批量作案）造成直接资金损失，
但粗放拦截会误伤正常用户导致流失。核心矛盾：**欺诈要少漏，正常用户要少误伤**。
本项目目标：在误杀率 ≤0.1% 的硬约束下，最大化欺诈拦截率，并给出可落地的
分层处置策略与可解释规则。

## 2. 数据与标签口径

- 数据集：Kaggle IEEE-CIS Fraud Detection；train_transaction 59 万笔 × 394 列，
  train_identity 14 万 × 41 列，按 TransactionID **左连接**（保留无设备指纹的交易）。
- 标签：`isFraud`，整体欺诈率 **X.XX%**（见 run_log 阶段 0）。
- **标签滞后局限声明**：真实业务中欺诈标签来自事后拒付/投诉，存在数周滞后，
  近期样本标签可能"假阴性"（欺诈还没被标记）。本项目用比赛提供的静态标签，
  无法校正此偏差，结论外推到线上时需打折——这是所有反欺诈项目的共同局限，
  主动声明比被面试官指出来好。

## 3. 欺诈模式假设与验证

> 每条假设按"假设 → 验证方法 → 结论 → 图"填写。

### 假设 A：设备/卡聚集

- 假设：同一 uid（卡+地址+邮箱）高频交易的欺诈率显著更高。
- 验证：uid_cnt 分箱欺诈率（1 次 / 2~3 次 / 4~10 次 / >10 次）。
- 结论：【填写：哪个分箱欺诈率最高，是整体的几倍】
- 图：`figures/03_uid_cnt_fraud_rate.png`

### 假设 B：金额分布异常

- 假设：欺诈金额分布异于正常，整数金额占比更高（脚本批量作案特征）。
- 结论：【填写：欺诈/正常的整数金额占比分别是多少】
- 图：`figures/02_amt_distribution.png`

### 假设 C：无设备指纹欺诈率更高

- 假设：identity 缺失的交易欺诈率显著更高。
- 结论：【填写：有无 identity 的欺诈率对比】
- 图：`figures/02_identity_missing.png`

### 补充观察

- 小时模式：【填写】图：`figures/02_hourly_fraud_rate.png`
- 邮箱域名：【填写】图：`figures/02_emaildomain_top15.png`
- 距离 dist1：【填写】图：`figures/02_dist1_binned.png`

## 4. 特征工程

- **UID 构造**：`uid = card1_card2_addr1_P_emaildomain`，共 X 万个实体，
  平均每实体 X 笔。
- **聚合特征**：uid_cnt / uid_amt_mean / uid_amt_std / amt_to_uid_mean。
- **口径声明**：严格应为"截至当前时刻"的历史统计；本项目用全训练期统计的
  简化版，OOT 切分已把未来样本挡在测试集外，泄漏有限但不为零（见 03 脚本注释）。
- **冷启动发现**：te 中新 uid 占 X.X%，新 uid 欺诈率 X.XX% vs 老 uid X.XX%
  ——结论：【新用户更危险还是更安全？这对新客策略的含义是什么】。

## 5. 模型与评估

- 模型：LightGBM（n_estimators=1500，lr=0.02，num_leaves=64，
  scale_pos_weight=8，行/列采样各 0.8），OOT te 上早停，最佳迭代 X 轮。
- **PR-AUC（主指标）= X.XXXX**；ROC-AUC（参考）= X.XXXX。
  图：`figures/05_pr_curve.png`
- **业务化口径**：阈值 = 预测分 99.9% 分位（FPR≈0.1%，实际 X.XXX%）下，
  **欺诈拦截率 = XX.X%**（X 笔被拦 / 共 X 笔），拦截精准率 XX.X%。
- **95% 置信区间**（bootstrap 1000 次）：拦截率 ∈ [XX.X%, XX.X%]，
  区间宽度 X.X% —— 诚实说明稳定性。
- **OOT vs 随机切分对比**：【填写：随机切分指标虚高了多少，说明什么】。
- **切分点敏感性**（0.75/0.80/0.85 重训）：PR-AUC 分别为 X/X/X，
  波动【大/小】，说明模型对时间漂移【敏感/稳健】。

## 6. 三级处置矩阵与成本权衡

- 成本假设：拦截 1 笔欺诈避免损失 = 交易金额；误杀 1 笔正常 = 50 元；
  二次验证 = 5 元/笔（验证挽回 90% 正常用户、放走 10% 欺诈）。
- 最优阈值对：**T1 = X.XXXX，T2 = X.XXXX**，回测净收益 **XX 万元**。
- 三级处置表：

| 分层 | 规则 | 交易占比 | 欺诈覆盖率 | 误杀数 |
|---|---|---|---|---|
| 高风险 | 分数 ≥ T1 → 拦截 | X.XX% | XX.X% | X |
| 中风险 | T2 ≤ 分数 < T1 → 二次验证 | X.XX% | X 笔欺诈 | — |
| 低风险 | 分数 < T2 → 放行 | XX.X% | — | — |

- 回测句式：拦截层覆盖 X% 欺诈（拦截率），误杀率 X%；二次验证层覆盖
  X% 交易，预计挽回误杀 X%。
- 图：`figures/06_net_benefit_heatmap.png`；全部扫描结果：`tier_scan_results.csv`。
- 敏感性讨论：【若误杀成本从 50 涨到 200，最优 T1 会怎么移动？为什么】。

## 7. 规则提炼

- 方法：SHAP 定位 Top20 特征 → 在模型高分（≥T1）样本上训练深度 3 决策树
  → 逐条规则在全量 te 独立验证精准率/覆盖率。
- 图：`figures/07_shap_summary.png`；规则表：`rules_summary.csv`。

| # | 规则文本 | 精准率 | 覆盖率 |
|---|---|---|---|
| 1 | 【填写】 | XX.X% | X.X% |
| 2 | 【填写】 | XX.X% | X.X% |
| 3 | 【填写】 | XX.X% | X.X% |

- 业务解读：【这些规则反映了什么作案手法？哪些可以直接进拦截系统，
  哪些只能做灰名单】。

## 8. 对抗演化推演

风控是攻防对抗，上线即开始失效。预案：

- **特征 PSI 监控**：对 Top20 特征逐周计算 PSI，PSI>0.2 告警漂移并触发重训。
- **规则轮换**：欺诈团伙会试探规则边界（如把金额从整百改成 99.9），
  规则要有备选池并按精准率衰减自动轮换。
- **标签滞后**：新欺诈模式从出现到有标签有数周空窗，期间靠无监督信号
  （异常聚集、设备指纹突变）兜底。
- **冷启动**：新 uid 欺诈率【高/低】于老用户，新客策略应【收紧/放开】
  二次验证门槛，并用实时行为序列补足无历史聚合特征的问题。

## 9. 简历 bullet（5 条，数字位待填）

1. 基于 Kaggle IEEE-CIS 数据集（59 万笔 × 394 列）完成反欺诈全流程项目，
   按时间 OOT 切分（8:2）评估，PR-AUC 达 X.XX。
2. 构造"卡+地址+邮箱"UID 实体聚合特征，验证设备聚集假设：高频 uid 欺诈率
   为整体 X 倍；发现冷启动新 uid 欺诈率 X 倍于老用户。
3. 在 FPR≈0.1% 的线上硬约束下实现欺诈拦截率 XX.X%（bootstrap 95% CI
   [XX.X%, XX.X%]），并通过 3 组切分点重训验证时间稳健性。
4. 设计三级处置矩阵（拦截/二次验证/放行），以净收益为目标扫描阈值对，
   回测净收益 XX 万元，总交易干预率仅 X.X%。
5. 用 SHAP + 深度 3 决策树把黑盒模型提炼为 X 条可读规则，最优规则精准率
   XX.X%、覆盖 X.X% 欺诈，可直接配置进拦截系统。

## 10. 面试防御清单（Q&A）

1. **Q：为什么用 OOT 切分不用随机切分？**
   A：模型上线面对的是未来交易。随机切分会让同一 uid 的行为跨期出现在
   训练/测试两侧造成泄漏、指标虚高；OOT 模拟真实上线，指标可信。我做了
   0.75/0.8/0.85 三组切分点重训验证稳健性。
2. **Q：为什么主指标是 PR-AUC 而不是 ROC-AUC？**
   A：欺诈率约 3.5%，极度不平衡下 ROC-AUC 被海量"容易判对的正常样本"
   撑高；PR 曲线只关心欺诈类的精准率/召回率，更诚实。业务上我们报的是
   FPR≈0.1% 阈值下的拦截率，并给 bootstrap 置信区间。
3. **Q：类别不平衡怎么处理的？**
   A：scale_pos_weight=8 给正类加权；评估不看 accuracy 看 PR 口径；
   阈值不按 0.5 而按业务 FPR 约束倒推，并在策略层用三级处置消化
   分数中段的不确定性。
4. **Q：为什么左连接 transaction 和 identity？**
   A：identity 只覆盖约 1/4 交易，内连接会丢掉 3/4 样本；且"无设备指纹"
   本身是强风险信号（假设 C 验证了缺失组欺诈率更高），丢掉等于丢掉信息。
5. **Q：uid 聚合特征有未来信息泄漏吗？**
   A：有轻微泄漏——我用了全训练期统计的简化口径。严格做法是"截至当前
   时刻"的滚动统计；OOT 切分已把未来样本挡在 te 外，泄漏有限，报告中
   已显式声明，生产实现会用滚动窗口或特征平台修正。
6. **Q：为什么用 GBDT 不用评分卡？**
   A：①欺诈模式强非线性、强交叉，树模型天然捕捉；②394 列脏数据（大量
   缺失/高基数类别），GBDT 无需填充与分箱，迭代快；③欺诈模式快速漂移，
   GBDT 可每日重训。评分卡的可解释性优势用 SHAP+规则提炼补足。
7. **Q：你的拦截率置信区间多宽？只报点估计不行吗？**
   A：te 上欺诈样本只有几千笔，点估计有运气成分。bootstrap 1000 次得到
   95% CI [XX.X%, XX.X%]，宽度 X.X%。只报点估计会给业务错误预期；
   区间宽了就加数据或延长观察窗，这才是诚实的工程态度。
